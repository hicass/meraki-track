{
  "name": "meraki-track",
  "version": "0.1.0",
  "private": true,
  "description": "Job Application Tracker - Full Stack Application",
  "type": "module",
  "scripts": {
    "dev": "concurrently --prefix none \"npm run frontend\" \"npm run backend\"",
    "frontend": "concurrently --prefix '[backend]' --prefixColors green \"nodemon --watch backend/src/server.ts --exec tsx backend/src/server.ts\"",
    "backend": "concurrently --prefix '[frontend]' --prefixColors blue \"cd frontend && npm run dev --turbopack\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
